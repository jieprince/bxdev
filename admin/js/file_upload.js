var $j = jQuery.noConflict();
	
$j(document).ready(function () {
	/*
	var jqXHRData = new Array();
	$("#btnFileUpload").on('click', function () {
	    $(jqXHRData).submit();
	})
	
	var policy_id = $j("input#policy_id").val();
	var policy_no = $j("input#policy_no").val();
	

	//alert("policy_id: "+policy_id);
	
	
	$j("#input_policy_pdf").fileupload({
		url:"policy.php?act=policy_pdf_upload",//文件上传地址，当然也可以直接写在input的data-url属性内
		maxFileSize: 5000000,
		dataType: 'json',
		formData:{policy_id:policy_id,policy_no:policy_no},//如果需要额外添加参数可以在这里添加
		done:function(e,data){
			if (data.result.msg != "OK") {}
		    }
		});
		*/

});